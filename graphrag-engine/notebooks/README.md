# notebooks

Notebook đánh giá, không phải code sản phẩm.

- `eval_retrieval_quality.ipynb` — so sánh recall/precision giữa vector-only và hybrid retrieval trên một tập câu hỏi test tự xây (nên gồm cả câu hỏi tra cứu đơn giản lẫn câu hỏi multi-hop quan hệ).
